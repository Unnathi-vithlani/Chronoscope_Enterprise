version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///chronoscope.db
      - DEMO_MODE=True
    volumes:
      - ./data:/app/data
